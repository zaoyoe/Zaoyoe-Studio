# AI 工作台积分计费 V2 执行文档

## 执行目标

本轮将 AI 工作台积分模式与钱包账务收口为同一条可核对链路：任务提交时完成余额预授权，成功后按实际金额结算，失败或取消时释放预授权；钱包保留六位小数并正确展示正负号；图片、文本、视频和提示词反推使用可识别的流水名称。

“我的 API”模式继续不扣本站积分，也不生成站内积分流水。

## 本轮边界

- 包含：执行文档、六位小数传输与展示、流水语义、原子预授权/结算/释放、工作台接入、历史审计脚本、测试和运维说明。
- 不包含：生产部署、生产环境变量切换、历史零扣费自动追扣、积分汇率或毛利参数调整。
- 历史追扣、汇率调整和额外体验优化只形成管理员待决项，不在本轮自动执行。

## 进度

| 里程碑 | 权重 | 状态 | 完成内容 | 下一步 |
| --- | ---: | --- | --- | --- |
| 1. 执行文档与基线 | 10% | 已完成 | 已固化范围、生产基线、验收、灰度和停止条件 | 无 |
| 2. 六位积分展示 | 15% | 已完成 | 钱包接口、PointsService 和钱包组件保留六位精度并自适应展示 | 无 |
| 3. 工作台流水语义 | 10% | 已完成 | 即时扣费和延迟对账均按任务模式生成流水名称 | 无 |
| 4. 原子授权结算 RPC | 20% | 已完成 | 已新增预授权表、任务级锁、三组 RPC 和服务端调用封装 | 无 |
| 5. 图文视频计费接入 | 20% | 已完成 | 队列、流式文本、延迟账单、失败、超时和取消均已接入 V2 | 无 |
| 6. 对账审计与测试 | 15% | 已完成 | 已新增 V2 微积分专项测试和只读审计脚本；近 30 天 287 个任务完成基线审计 | 无 |
| 7. 验证与收口 | 10% | 已完成 | 相关 JavaScript 语法检查、差异检查和六组聚焦回归全部通过，共 261 项测试、0 失败 | 无；本轮按停止条件收口 |

当前总进度：100%。

## 已确认基线

- AI 任务、积分余额和积分流水数据库字段均支持六位小数，但钱包读取和展示链路仍会舍入到两位。
- 当前所有工作台扣费流水统一写成“AI 图片生成”，无法区分文本、图片和视频。
- 旧扣费 RPC 允许部分扣款或零扣款，工作台调用方还可能把零扣款误报成预计扣款。
- 当前缺少微积分、余额不足、同任务并发结算和预授权释放测试。

## 验收条件

1. `-0.000233` 在钱包列表和详情中保持负号，并显示为 `-0.000233`。
2. 积分模式任务提交前必须获得足额预授权；余额不足时不调用上游。
3. 成功任务的 `charged_points` 必须等于对应负流水绝对值。
4. 失败或取消任务释放全部预授权，最终 `charged_points = 0` 且无可见负流水。
5. 同一任务重复授权、结算或释放必须幂等，不能重复扣费。
6. 图片、文本对话、视频和提示词反推在钱包中使用不同流水名称。
7. “我的 API”模式不改变站内余额，不生成站内积分流水。
8. 聚焦测试全部通过，工作树中不改动用户已有的无关文件。

## 明确停止条件

满足上述八项验收条件、执行文档更新到 100%、聚焦测试通过后，本轮立即收口。任何不影响正确性、安全性或账务一致性的进一步优化只进入“管理员待决项”，不继续扩张实现范围。

## 灰度与回滚约束

- 新账务链路由 `AI_WORKBENCH_BILLING_V2_ENABLED=true` 显式开启；默认保持关闭。
- 开启前必须先执行数据库迁移，并确认没有遗留的 `queued` / `running` 积分任务。
- V2 开启后，如果预授权或结算 RPC 不可用，积分任务必须失败关闭，不能退回旧的部分扣款路径。
- 钱包精度和流水展示修复不依赖 V2 开关，可独立上线。

## 管理员待决项

- `points_per_usd` 是否从当前 `1` 调整为按站点配置的汇率与毛利倍率。
- 近 30 天审计发现的 104 条历史零扣费文本任务，是只保留审计结果，还是对可恢复上游账单的任务人工补扣。
- 微积分余额在钱包顶部采用自适应六位展示，还是两位主值加精确值提示。

## 执行记录

- 2026-07-15：创建执行文档，锁定本轮边界、验收条件、灰度开关和停止条件。
- 2026-07-15：钱包读取链路由两位小数升级到六位，并为微积分启用自适应六位展示。
- 2026-07-15：新增工作台流水名称映射，并接入即时扣费与延迟 Sub2API 对账路径。
- 2026-07-15：新增 Billing V2 数据库迁移和服务端调用封装；预授权采用隐藏流水，结算后更新为最终可见流水，释放后恢复余额。
- 2026-07-15：图片、视频、文本、延迟账单、失败、超时和取消路径完成 V2 接入；既有聚焦回归保持通过。
- 2026-07-15：新增只读账务审计脚本与 Billing V2 专项测试。近 30 天 287 个任务中发现 104 个成功文本任务有 token 但扣费为零；未发现账单缺失、金额不一致、重复可见扣款或失败任务残留扣款。审计输出仅含汇总指标，不含用户身份、提示词或密钥。
- 2026-07-15：完成相关 JavaScript 语法检查、`git diff --check` 和六组聚焦回归，共 261 项测试、0 失败。本轮未执行数据库迁移、生产开关或部署，按明确停止条件收口。

## 收口结论

本轮计划内实现与验证已完成，总进度 100%。Billing V2 仍默认关闭；生产启用必须由管理员另行批准，并严格按“先迁移、再清空遗留任务、后开启开关、最后持续审计”的顺序执行。管理员待决项不在本轮继续实施。

## 生产上线阶段

基础部署进度：100%。PR #564 已合并到 `main`，Vercel production、KVM4 Verify Server 和 KVM4 Sub2API 均已部署 `201dc8ec` 并通过主机级健康检查；Billing V2 在该阶段保持关闭。

## Billing V2 灰度启用阶段

| 步骤 | 权重 | 状态 | 完成内容 | 下一步 |
| --- | ---: | --- | --- | --- |
| 1. 启用前只读复验 | 10% | 已完成 | 活动积分任务和授权中预留均为 0；三组 Billing V2 RPC 仅允许 `service_role` 执行 | 无 |
| 2. 灰度配置与部署 | 40% | 已完成 | PR #565 已合并；Vercel Production、KVM4 Verify Server 和 KVM4 Sub2API 均部署 `67f6fec4` 并通过健康检查 | 无 |
| 3. 运行时开关复验 | 10% | 已完成 | Verify Server 和 AI Worker 均加载 Billing V2 开关及动态预授权 `1` | 无 |
| 4. 金丝雀前残留检查 | 5% | 已完成 | 2026-07-15 23:52 后任务、流水和预授权均为 0，未登录尝试没有进入账务链路 | 无 |
| 5. 图文与释放金丝雀 | 15% | 已完成账务核验 | 文本成功扣费 `0.000203`、1K 单图成功扣费 `8`，任务金额、可见负流水和 `settled` 预授权一致；两次失败任务均释放 1 积分且无负流水 | 钱包 UI 复验发现隐藏释放流水仍显示为 `+0` |
| 6. 金丝雀与全量审计 | 5% | 已完成 | 4 个灰度任务无金额不一致、重复扣款、失败扣款或残留预授权；近 30 天审计仅保留既有 104 条历史零扣费文本基线 | 修复部署后再次执行同级审计 |
| 7. 隐藏流水修复与复验 | 15% | 已完成 | PR #566 已合并；钱包浏览和搜索接口统一过滤 `is_visible=false`，兼容旧字段回退；51 项聚焦回归、三条部署链路、钱包 UI 和修复后文本金丝雀均通过 | 无；按收口点结束本轮 |

当前灰度进度：100%。账务原子性、图文微扣费、失败释放、钱包可见性和全量审计均已通过；Billing V2 已在生产重新启用。修复后文本任务扣费 `0.000339`，任务金额、`settled` 预授权和钱包负流水一致；钱包仍显示“AI 文本对话”与“AI 图片生成”的正确负流水，但不再显示释放预授权或 `+0 分`。

2026-07-16 00:21 已执行灰度暂停：Vercel Production 回滚到禁用版本 `zaoyoe-h945qllzz-zaoyoes-projects.vercel.app` 并确认 `Ready`；KVM4 使用 `/opt/zaoyoe-verify-server/.env.billing-v2-backup-20260715153714` 恢复环境，Verify Server 与 AI Worker 运行时均确认 V2 disabled，`/healthz` 和相关容器正常。下一次启用必须来自修复合并后的最新 `main`。

2026-07-16 00:47 完成灰度收口：PR #566 合并为 `d387c92a`；Vercel Production `dpl_84oLk26dNZsg4BMBDgY7NRmUJoj7` 为 `Ready`，KVM4 Verify Server 工作流 `29432653421` 与 Sub2API 工作流 `29432653447` 均成功，两份 `.current-release` 均等于 `d387c92a`，健康检查和相关容器正常。灰度任务范围审计异常为 0；近 30 天审计仅保留既有 104 条成功文本零扣费历史基线，不自动追扣。

本阶段在此明确收口。历史 104 条记录追扣、积分汇率调整和其他体验优化继续保留为管理员待决项，未获得新的管理员决定前不继续执行。

## 钱包精确余额悬浮提示迭代

本迭代解决钱包顶部余额固定显示两位时，用户无法直接确认微积分扣费是否真实生效的问题。数据库和账务链路继续以六位小数为权威值；总余额、付费余额和赠送余额主值保持两位，鼠标悬浮时通过原生提示展示六位精确值，并同步可访问性标签。

| 步骤 | 权重 | 状态 | 完成内容 | 下一步 |
| --- | ---: | --- | --- | --- |
| 1. 精度与展示基线 | 20% | 已完成 | 已确认生产余额 `18492.083459`，钱包两位主值只是显示取舍，不影响六位账务扣费 | 无 |
| 2. 精确余额提示实现 | 20% | 已完成 | 总余额、付费余额、赠送余额均增加六位精确悬浮提示、`data-exact-points` 和 `aria-label`；签到与补签刷新同步更新提示 | 无 |
| 3. 聚焦回归 | 20% | 已完成 | JavaScript/JSON 语法、差异格式、Billing V2 专项 7 项、受影响契约 212 项及全量安全回归 2631 项全部通过，0 失败 | 无 |
| 4. PR 合并 | 20% | 已完成 | PR #568 的预检、8 个安全分片、汇总检查和 Cloudflare Pages 全部通过，已合并为 `d652b8f3` | 无 |
| 5. 生产部署与页面验收 | 20% | 已完成 | Vercel Production `dpl_7AVoEywMiYjKwCfbG8gZ5Hymz9cu` Ready；KVM4 Verify 工作流 `29467284499` 重跑成功，Sub2API 工作流 `29467284530` 成功；两份 release 均为 `d652b8f3`，健康接口、容器、Billing V2 和生产钱包三组提示均通过 | 无；按停止点收口 |

当前迭代进度：100%。

验收结果：钱包主值仍显示 `18,492.08`；总余额和付费余额精确提示均为 `18,492.083459`，赠送余额精确提示为 `0.000000`；三组 `title`、`data-exact-points` 和 `aria-label` 一致。Verify Server 与 AI Worker 的 Billing V2 均保持 `true`，部署来自最新 `main`。

2026-07-16：完成本迭代收口。KVM4 Verify 首次工作流因 GitHub runner SSH 连接超时失败；确认 KVM4 本机和公开服务健康后，仅重跑失败工作流并成功。SSH 复验 `zaoyoe-verify-server`、AI Worker、Prompt Worker、Sub2API、PostgreSQL 和 Redis 均运行正常，其中有健康检查的容器均为 `healthy`。

本迭代不追扣既有 104 条历史零扣费文本记录，不调整积分汇率，也不扩张其他钱包体验优化。任何新增优化仅作为管理员待决项，由管理员决定是否另开迭代。

本迭代已达到 100% 并在此明确收口，不再继续追加优化。

## 钱包精确余额自定义提示迭代

本迭代仅替换上一轮精确余额的浏览器原生 `title` 提示：总余额、付费余额和赠送余额继续以两位小数显示主值，以六位小数展示权威精确值；账务精度、积分汇率、Billing V2 和历史流水处理均不变。

| 步骤 | 权重 | 状态 | 完成内容 | 下一步 |
| --- | ---: | --- | --- | --- |
| 1. 交互与边界确认 | 15% | 已完成 | 确认移除反应慢且样式不可控的原生 `title`；保留 `data-exact-points`、`aria-label`，并支持键盘聚焦 | 无 |
| 2. 自定义气泡实现 | 25% | 已完成 | 三组余额均使用自定义深浅色气泡和箭头；悬浮或键盘聚焦立即触发，110ms 淡入；移动端和减少动画模式已适配 | 无 |
| 3. 静态与回归验证 | 30% | 已完成 | `git diff --check`、2 项 JavaScript 语法检查、Billing V2 专项 7 项、受影响契约 212 项及全量安全回归 2633 项全部通过，0 失败 | 无 |
| 4. PR 合并 | 10% | 进行中 | 本地实现、缓存版本和契约测试已完成并通过 | 隔离提示词页面的并行改动，提交当前迭代，创建 PR 并在检查通过后合并 |
| 5. 生产部署与管理员验收 | 20% | 待执行 | 尚未开始 | 仅由最新 `main` 自动部署并验证 Vercel、KVM4 Verify、KVM4 Sub2API；生产钱包三组自定义提示由管理员手动验收 |

当前迭代进度：70%。

停止条件：PR 已合并且检查通过；Vercel Production 为 `Ready`；两条 KVM4 工作流成功且服务器 release、健康接口和容器正常；代码与自动化契约确认余额 DOM 不再写入 `title`，三组自定义提示均保留六位精确值、深浅色与键盘聚焦能力；Billing V2 保持启用。生产视觉效果交由管理员手动验收，不再由 Codex 调用浏览器测试。达到这些条件并将本节更新为 100% 后立即收口。

本迭代不调整账务计算、不追扣历史记录、不修改积分汇率。任何不影响上述验收条件的视觉或交互优化只列为管理员待决项，不在本轮继续扩张。
