# Meigen Gallery 采集器

这是 Gallery Import 的浏览器辅助采集器。

## 管理员使用

1. 在 Chrome 打开扩展程序页面，开启开发者模式。
2. 选择“加载已解压的扩展程序”，选择本目录。
3. 打开 Meigen 页面，正常浏览并滚动到需要采集的作品。
4. 点击浏览器右上角 `Meigen 导入`。
5. 点击 `采集当前页`。
6. 需要自动加载更多作品时，点击 `滚动采集`；想提前结束时点击 `停止滚动`。
7. 页面有下一页或更多按钮时，点击 `翻页采集`；想提前结束时点击 `停止翻页`。
8. 需要补齐详情时，点击 `补抓详情`；过程中可以 `暂停` / `继续`。
9. 如果有详情失败，点击 `重试失败`。
10. 点击 `送入队列`，然后回到 Admin Studio 的 Gallery / Import 查看队列。

送入队列时会先和当前提示词仓库做精确去重；已有提示词不会再次写入队列，弹窗会显示自动跳过数量。

如果送入队列失败，可以点击 `下载结果`，再回到 Gallery / Import 使用 `导入抓取结果`。

## 采集内容

- 一个提示词对应多张图片。
- 提示词、原作品链接、原作者昵称、原作者 ID、收藏量。
- 缺少提示词或来源时不会丢弃，会进入 Import 的待处理队列。
- 扩展 `0.1.1` 只会在 Admin Studio 页面注入登录态转发桥，不再覆盖主站其它页面。
