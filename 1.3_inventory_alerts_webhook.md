# 库存告警设置指南 (Phase 1.3)

为了实现“商品库存低于阈值时，自动向管理员发送 Telegram/企业微信 消息”，请按照以下步骤配置。

## 1. 部署 Laf 云函数 (Webhook 接收端)

1. 在 [Laf 云开发平台](https://laf.run) 创建一个新的云函数，命名为 `webhook-inventory-alert`。
2. 确保在左侧面板安装了 `axios` 依赖。
3. 粘贴以下代码，并将里面的 `YOUR_TELEGRAM_BOT_TOKEN`、`CHAT_ID` 或 `WECHAT_WEBHOOK_URL` 替换为你自己的配置：

```typescript
import cloud from '@lafjs/cloud'

export async function main(ctx: FunctionContext) {
  // 接收 Supabase 数据库 Webhook 发来的 payload
  const body = ctx.body;
  
  if (!body || !body.record) {
    return { code: 400, message: 'Invalid payload' }
  }

  // 从 shop_alerts 表的插入记录中提取信息
  const { product_name, stock_count, alert_threshold } = body.record;

  // 构建告警消息
  const message = `🚨【库存报警】🚨\n\n商品: ${product_name}\n当前库存: ${stock_count} 件\n警戒线: ${alert_threshold} 件\n\n请尽快登录后台补充卡密！`;
  console.log('触发告警:', message);

  const axios = require('axios');

  // ---------- 选项 1: Telegram 机器人推送 ----------
  const BOT_TOKEN = 'YOUR_TELEGRAM_BOT_TOKEN';
  const CHAT_ID = 'YOUR_TELEGRAM_CHAT_ID';
  
  if (BOT_TOKEN && BOT_TOKEN !== 'YOUR_TELEGRAM_BOT_TOKEN') {
    try {
      await axios.post(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
        chat_id: CHAT_ID,
        text: message
      });
    } catch (e) {
      console.error('Telegram push failed', e.message);
    }
  }

  // ---------- 选项 2: 企业微信/钉钉 Webhook 推送 ----------
  const WECOM_WEBHOOK = 'YOUR_WECHAT_WEBHOOK_URL';
  
  if (WECOM_WEBHOOK && WECOM_WEBHOOK !== 'YOUR_WECHAT_WEBHOOK_URL') {
    try {
      await axios.post(WECOM_WEBHOOK, {
        msgtype: 'text',
        text: { content: message }
      });
    } catch (e) {
      console.error('WeCom push failed', e.message);
    }
  }

  return { code: 0, message: 'Alert processed' }
}
```

4. 保存并发布该函数。
5. 复制该函数的访问 URL（例如 `https://xyz.laf.run/webhook-inventory-alert`）。

## 2. 在 Supabase 配置 Database Webhook

每次有告警记录插入 `shop_alerts` 表时，通知上述 Laf 函数：

1. 打开 Supabase Studio -> **Database** -> **Webhooks**
2. 点击 **Enable Webhooks** (如果尚未启用)，然后点击 **Create a new Hook**。
3. **Name**: `Send Inventory Alert`
4. **Table**: `shop_alerts`
5. **Events**: 勾选 `INSERT`
6. **Type**: `HTTP Request`
7. **HTTP Request > Method**: `POST`
8. **HTTP Request > URL**: 粘贴你在步骤1复制的 Laf 函数 URL。
9. 点击 **Create webhook** 保存。

## 3. 测试流程
1. 在 Supabase 的 `shop_products` 将某件商品的 `stock_count` 设置为 2，`alert_threshold` 设置为 2（表示低于等于 2 将告警）。
2. 在前端购买 1 件该商品，触发真正的减库存 (`stock_count` 变成 1，触发 `tr_low_stock_alert`)。
3. 去查看 Telegram 消息，您会立刻收到库存报警！
